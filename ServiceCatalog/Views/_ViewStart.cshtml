@{
    var env = Session["ENV"]?.ToString() ?? "prod";

    if (env == "prod")
    {
        Layout = "~/Views/Shared/_LayoutMain.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_LayoutDemoApi.cshtml";
    }
}